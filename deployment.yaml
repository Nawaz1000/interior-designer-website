apiVersion: apps/v1
kind: Deployment
metadata:
  name: excellence-website-deployment
  labels:
    app: excellence-website
spec:
  replicas: 1
  selector:
    matchLabels:
      app: excellence-website
  template:
    metadata:
      labels:
        app: excellence-website
    spec:
      containers:
        - name: excellence-website
          image: nawazs786/real-state-website-project-final-one:latest
          imagePullPolicy: IfNotPresent # Useful for local testing with Minikube/Docker Desktop
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: NODE_ENV
              value: "production"
          # Uncomment and set if you want to override the MongoDB URI
          # - name: MONGODB_URI
          #   value: "mongodb+srv://..."

---
apiVersion: v1
kind: Service
metadata:
  name: excellence-website-service
spec:
  selector:
    app: excellence-website
  type: ClusterIP # Change to NodePort or ClusterIP based on your cluster (e.g., Minikube usually needs `minikube tunnel` for LoadBalancer)
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
